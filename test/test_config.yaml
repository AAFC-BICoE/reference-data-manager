## This configuration file is for test purposes. Set root_folder and other attributes accordingly.
root_folder: "../out/test/"
backup_folder: "../out/test/backup/"
readme_file: "README+"
download_retry_num: 3
connection_retry_num: 3

ncbi:
    destination_folder: "ncbi/"

    blast_db:
        destination_folder: "blast/"
        ftp: "ftp.ncbi.nlm.nih.gov"
        ftp_dir: "blast/db/"
        user: "anonymous"
        password: "anonymous"
        info_file_name: "README"


## Keep below structure - this format is expected by python's logging module
logging:

    version: 1
    disable_existing_loggers: true

    formatters:
        briefFormatter:
            format: '%(levelname)s: %(message)s'
        preciseFormatter:
            format: '%(asctime)s - %(module)s - %(levelname)s: %(message)s'
            datefmt: '%Y/%m/%d %H:%M:%S'

    handlers:
        console:
            class: logging.StreamHandler
            formatter: briefFormatter
            level: ERROR
            stream: ext://sys.stdout
        file:
            class : logging.FileHandler
            formatter: preciseFormatter
            level: DEBUG
            filename: test.log

    root:
        level: DEBUG
        handlers: [console, file]
